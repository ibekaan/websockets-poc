import React, { FC } from "react";

export const SendNotificationPage: FC = () => {
  return (
    <div>
      <h1>Welcome USER</h1>
      <button>Trigger notification for other users</button>
    </div>
  );
};
